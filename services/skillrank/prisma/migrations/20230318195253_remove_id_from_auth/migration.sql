/*
  Warnings:

  - The primary key for the `skillrank` table will be changed. If it partially fails, the table could be left without primary key constraint.

*/
-- AlterTable
ALTER TABLE `skillrank` DROP PRIMARY KEY;
